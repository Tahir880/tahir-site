'use client';import {Link} from '@/i18n/navigation';;import {useTranslations,useLocale} from 'next-intl';import {usePathname} from 'next/navigation';import clsx from 'clsx';
export default function NavBar(){const t=useTranslations('nav');const tc=useTranslations('common');const locale=useLocale();const pathname=usePathname();const links=[{href:'/',label:t('home')},{href:'/music',label:t('music')},{href:'/epk',label:t('epk')},{href:'/contact',label:t('contact')}];return(<div className='sticky top-0 z-50 border-b border-white/10 bg-black/30 backdrop-blur'><div className='container flex items-center justify-between py-3'><Link href='/' className='no-underline'><span className='font-bold'>{tc('brand')}<span className='text-brand-accent'>•</span></span></Link><nav className='hidden md:flex gap-6'>{links.map(l=>{const href=`/${locale}${l.href}`.replace('//','/');const active=pathname===href;return(<Link key={l.href} href={l.href} className={clsx('no-underline',active&&'text-brand-accent')}>{l.label}</Link>);})}</nav></div></div>);}
